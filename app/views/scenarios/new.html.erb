<!DOCTYPE html>
<html>
  <body>
          <style> 
                   @font-face {
                                 font-family: myFirstFont;
                                  src: url(sansation_light.woff);
                              }
                  @font-face {
                                font-family: myFirstFont;
                                src: url(sansation_bold.woff);
                                font-weight: bold;
                              }
                        div {
                                font-family: myFirstFont;
                            }
              </style>
      <h3><i>New Scenario</i></h3>
    <div class="z-depth-5 container hoverable">
        <%= form_for @scenario do |f| %>
      <div class="field">
        <%= f.label :content %><br>
        <%= f.text_area :content, size: "60x12" %><br>
      </div>
        <%= f.submit %>
        <% end %> 
    </div>
  </body>
</html>
